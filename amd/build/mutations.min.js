/**
 * Format topics mutations.
 *
 * An instance of this class will be used to add custom mutations to the course editor.
 * To make sure the addMutations method find the proper functions, all functions must
 * be declared as class attributes, not a simple methods. The reason is because many
 * plugins can add extra mutations to the course editor.
 *
 * @module     format_ludilearn/mutations
 * @copyright  2025 Pimenko <contact@pimenko.com>
 * @author     Jordan Kesraoui
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("format_ludilearn/mutations",["core_courseformat/courseeditor","core_courseformat/local/courseeditor/mutations","core_courseformat/local/content/actions"],(function(CourseEditor,DefaultMutations,CourseActions){class LudilearnMutations extends DefaultMutations{cmGamify=async function(stateManager,cmids){stateManager.setReadOnly(!1),cmids.forEach((id=>{const element=stateManager.get("cm",id);element&&(element.gamified=!0)}));const course=stateManager.get("course"),updates=await this._callEditWebservice("cm_gamify",course.id,cmids);stateManager.processUpdates(updates,{})};cmNotgamify=async function(stateManager,cmids){stateManager.setReadOnly(!1),cmids.forEach((id=>{const element=stateManager.get("cm",id);element&&(element.gamified=!1)}));const course=stateManager.get("course"),updates=await this._callEditWebservice("cm_notgamify",course.id,cmids);stateManager.processUpdates(updates,{})}}return{init:function(){CourseEditor.getCurrentCourseEditor().addMutations(new LudilearnMutations),CourseActions.addActions({cmGamify:"cmGamify",cmNotgamify:"cmNotgamify"})}}}));

//# sourceMappingURL=mutations.min.js.map